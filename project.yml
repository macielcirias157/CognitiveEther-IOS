name: CognitiveEther
options:
  bundleIdPrefix: com.cognitive
targets:
  CognitiveEther:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: Sources
      - path: Resources
    settings:
      base:
        INFOPLIST_FILE: Sources/Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: com.cognitive.ether
        DEVELOPMENT_TEAM: "" # Set this in GitHub Secrets or leave for manual signing
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        MARKETING_VERSION: 1.0.0
        CURRENT_PROJECT_VERSION: 1
    dependencies:
      # Add dependencies here
